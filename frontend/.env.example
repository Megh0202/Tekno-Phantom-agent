# Leave empty to use same-origin (recommended behind NGINX/reverse proxy).
NEXT_PUBLIC_API_BASE_URL=
# Leave blank for normal email/password UI auth. Set only if you intentionally want admin-token bypass.
NEXT_PUBLIC_ADMIN_API_TOKEN=
NEXT_PUBLIC_SHOW_ADVANCED_INPUTS=false
